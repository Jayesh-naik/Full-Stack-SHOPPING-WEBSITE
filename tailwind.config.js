/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    // This line tells Tailwind to scan all JS, JSX, TS, and TSX files in the src/ folder.
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};